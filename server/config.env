# ExamShield License Server Configuration
# Copy this file to .env and update with your production values
# NEVER commit .env to git!

# ===========================================
# SMTP Settings for Email Delivery
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# For Gmail: Use App Password (not regular password)
# Get App Password: Google Account → Security → 2-Step Verification → App Passwords

# ===========================================
# Security Secrets
# ===========================================
# Generate strong secrets using:
# Linux/Mac: openssl rand -hex 32
# Windows PowerShell: -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
WEBHOOK_SECRET=change-me-secret-key-here
ADMIN_SECRET=admin-secret-change-me

# ===========================================
# Data Directory
# ===========================================
# Local development: ./data
# Render deployment: /opt/render/project/src/server/data
# VPS deployment: /opt/examshield-license/data
ES_DATA_DIR=./data

# ===========================================
# Flask Server Settings
# ===========================================
FLASK_HOST=0.0.0.0
FLASK_PORT=8080

# Note: Render/Heroku automatically set PORT environment variable
# The server code uses PORT if available, otherwise FLASK_PORT

# ===========================================
# Payment Provider (Razorpay)
# ===========================================
# Get from Razorpay Dashboard → Settings → API Keys
# Test keys: rzp_test_xxxxx
# Live keys: rzp_live_xxxxx
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# ===========================================
# Optional Settings
# ===========================================
# Enable public license count endpoint (for portfolio widget)
PUBLIC_REPORTS_ENABLED=false

# Debug Mode (Disable in production)
DEBUG=false

# ===========================================
# Production URLs
# ===========================================
# License Server URL (for client verification)
# Update this after deployment:
# LICENSE_SERVER_URL=https://examshield-license-erver.onrender.com

# Shop URL (for email links)
# SHOP_URL=https://adulsportfolio.vercel.app/shop

